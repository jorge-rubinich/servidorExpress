<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" type="text/css" href="index.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

    <title>{{title}}</title>
</head>

<body>
    <div class="container" >
        <div class="row" style="background-color: #dddaca">
            <div class="col-4">
                <a href="/"><img src="/imgs/logo.png" alt="Bocha Store"></a>
            </div>
            <div class="col-4 text-center" >

                <div class="">
				<b>Hola,</b> 
                {{#if user}}
                    <b>{{user.first_name}}</b> 
                    {{#if user.admin}}
                        (Admin)
                    {{/if}}
                {{else}}
                    Inicia tu sesión 
                {{/if}}
			    </div>
                <div>
                    {{#if user}}
                        <a href="#" onclick="signOut()" class="sessionButton" >Salir</a>
                    {{else}}
				        <a href="#" onclick="signIn()" class="sessionButton">Ingresá</a>  |  
                        <a href="#"onclick="signUp()" class="sessionButton">Registrate</a>

                    {{/if}}
			    </div>

            </div>
            <div class="col-4 text-right">
                <button onclick="goToCart()"><img src="/imgs/cart.svg" alt="Mi carrito"></button>
            </div>


        </div>
        {{{body}}}

        
</body>

</html>
<script src="/js/main.js"></script>
